# Artifact MCP v1.7.1

The protected `v1.7.0` tag did not publish a GitHub Release or stable image. Version 1.7.1 is the
first distributable release in this feature line.

## Schema

- Upgrading from the public v1.6.1 release migrates schema 23 to schema 32.
- Migrations are forward-only and run before the listener becomes ready. Frozen historical
  fixtures cover every boundary through schema 32, reopen idempotency, corruption refusal, and
  tenant isolation.
- Schema 32 adds anchor-v2 evidence fields for feedback while retaining every legacy anchor and
  reply shape.

## Release highlights

- The artifact library now uses one top toolbar for search, quick views, organization and category
  dropdowns, sorting, and grid/list layout. The old left filter rail is removed, cards align to a
  common height, and library state returns after viewing an artifact.
- The artifact viewer now prioritizes the sandboxed artifact instead of repeating the Gallery
  masthead. Compact menus and a trusted inspector own feedback, details, sharing, history,
  audience, reactions, theme, download, and deletion.
- Anchor v2 persists kind, DOM path, node id, quote, page, and normalized point or region evidence
  through Rust, Node, HTTP, and MCP projections. The browser adds marker previews, a positioned
  composer, threaded replies, stale-revision handling, and deterministic Copy prompt output.
- Verified publisher ownership now governs member Hide/Show and Delete controls. Administrators
  can fill or edit publisher-key label, role, and verified owner metadata without changing the key.
- Organization-scoped Discord notification threads and guarded two-way replies remain opt-in.
  Artifact MCP stays canonical when Discord is unavailable.

## Deployment notes

1. Create and test a backup of the database, artifact bodies, previews, and deployment
   configuration before starting the new binary or image.
2. Record the currently running binary or immutable image digest as the rollback target.
3. Verify `WEBHOOK_ENC_KEY` and `AUDIT_LEDGER_HMAC_KEY` are present without printing or sourcing
   either value. `WEBHOOK_ENC_KEY` must remain a canonical base64-encoded 32-byte key.
4. Keep `DISCORD_INBOUND_ENABLED` absent or set to `0` for the initial rollout.
5. Deploy only the checksummed, attested binary or immutable OCI digest from
   `release-manifest.json`; do not deploy a mutable tag or rebuild on the target host.
6. Wait for `/health` to return `{"status":"ok"}` and verify that the database reports schema 32.
7. Confirm Settings returns only redacted organization Discord readiness and that browser token
   fields remain empty after save and reload.
8. Confirm Artifact MCP feedback, publishing, and viewing remain available with Discord
   unavailable. Do not enable historical recovery unless one exact selected-webhook notification
   can be matched to the canonical artifact URL.
9. Enable inbound synchronization only in a later, explicit canary after the Discord application
   has the required gateway intents and least-privilege channel permissions.

## Configuration changes

- Discord bot credentials are per organization, write-only in the browser, encrypted at rest, and
  never returned by an API.
- `DISCORD_BOT_TOKEN` is migration fallback only. Do not display, log, copy into release evidence,
  or treat it as the ongoing organization credential.
- Organization settings control the default outbound-threading policy. Artifacts can explicitly
  keep discussion in Artifact MCP or inherit that organization default; guarded two-way sync
  also requires the organization credential and readiness boundary.
- Historical recovery is bound to the exact selected notification webhook identity and canonical
  artifact URL. Missing, incomplete, or redacted history fails closed without creating duplicate
  notification floods. When several exact cards exist, recovery selects Discord's newest match.
- Artifact MCP remains canonical during Discord REST, Gateway, permission, rate-limit, and thread
  failures.
- The UI and anchor-v2 additions require no new environment variable. Existing feedback remains
  readable; new v2 fields are nullable for legacy rows and always project explicitly through HTTP
  and MCP.

## Compatibility and rollback

- Current MCP legacy-client behavior from v1.6.1 is retained alongside the newer MCP contract.
- Anchor-v2 persistence and projections are covered by Node/Rust parity checks. Legacy anchors,
  replies, and clients continue to use their existing shapes.
- The Node comparison runtime intentionally exposes a safe-unavailable projection for the
  Rust-owned organization credential and inbound Gateway capabilities.
- A binary-only or image-only rollback is unsafe after schema 32 has been opened. Stop the new
  release, restore both data and deployment configuration from the operator's pre-migration
  backup, and start the binary or immutable image digest recorded before deployment.
- Leave inbound synchronization disabled throughout rollback.

## Vulnerability exceptions

None.
