# Preset-only model deployment

Neil selected preset voices only after reviewing dual-model memory requirements.
`preset.yml` records the final single-CustomVoice model configuration.
`base-original.yml` records the previous Base configuration for rollback.
No two-model deployment or decoder-code modification was completed.
