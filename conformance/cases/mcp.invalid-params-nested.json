{
  "id": "mcp.invalid-params-nested",
  "tags": ["mcp", "contracts", "invariant-8"],
  "fixture": "empty-v21",
  "steps": [
    {
      "name": "nested-invalid file value returns -32602",
      "request": {
        "method": "POST",
        "path": "/mcp",
        "headers": { "authorization": "Bearer ${key_acme}", "content-type": "application/json" },
        "json": { "jsonrpc": "2.0", "id": 6, "method": "tools/call", "params": { "name": "publish_bundle", "arguments": { "files": { "index.html": 42 } } } }
      },
      "expect": { "status": 200, "body": { "mode": "canonical-json" } }
    }
  ]
}
