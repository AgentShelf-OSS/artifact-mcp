{
  "id": "mcp.invalid-params-missing",
  "tags": ["mcp", "contracts", "invariant-8"],
  "fixture": "empty-v21",
  "steps": [
    {
      "name": "missing required + unknown arg returns -32602",
      "request": {
        "method": "POST",
        "path": "/mcp",
        "headers": { "authorization": "Bearer ${key_acme}", "content-type": "application/json" },
        "json": { "jsonrpc": "2.0", "id": 5, "method": "tools/call", "params": { "name": "publish_artifact", "arguments": { "surprise": true } } }
      },
      "expect": { "status": 200, "body": { "mode": "canonical-json" } }
    }
  ]
}
