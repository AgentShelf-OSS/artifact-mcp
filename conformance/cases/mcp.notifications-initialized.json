{
  "id": "mcp.notifications-initialized",
  "tags": ["mcp", "contracts", "invariant-8"],
  "fixture": "empty-v21",
  "steps": [
    {
      "name": "notifications/initialized as notification yields 202",
      "request": {
        "method": "POST",
        "path": "/mcp",
        "headers": { "authorization": "Bearer ${key_acme}", "content-type": "application/json" },
        "json": { "jsonrpc": "2.0", "method": "notifications/initialized" }
      },
      "expect": { "status": 202 }
    }
  ]
}
